# fluent 队列计算

### 功能：
- 需要实现：能实现排队计算。并且能操作队列，还能记录已完成的任务。
- 初步想法：将主进程分为两个线程，  
1.管理队列  2. 管理计算  
管理队列的线程负责操作队列，即增删改查； 管理计算的线程负责拿走第一个任务，并运行计算
- 两个线程之间共用三个数据：已完成任务信息；正在进行任务信息；待处理任务信息。 
三个数据都需要有网盘备份（初步选择csv），正在进行和待处理需要有list对应在主程序里


### 界面
- 人需要接触的就是管理队列的线程，即管理三个数据，实现查看已完成和正在进行队列，
增删改查待处理队列
- 已完成队列隐藏在按键之下（不可编辑）；正在进行队列展示出来（不可编辑）；
已完成队列展现出来，并可以任意调整，最好是拖动改变, 退而求其次是上移下移。删除与增加各做一个按键。

### 拓展
- 进度条